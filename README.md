<<<<<<< HEAD
# Todo App

HTML, CSS, Vanilla JavaScript, Tailwind CDN으로 만든 간단한 Todo App입니다.

## 사용 기술

- HTML
- CSS
- Vanilla JavaScript
- Tailwind CDN

## 주요 기능

- Todo 추가
- Enter 키로 추가
- 빈 값 방지
- Todo 목록 표시
- 완료/미완료 변경
- Todo 삭제
- Total / Completed / Remaining 개수 표시
- localStorage 저장으로 새로고침 후 유지

## 실행 방법

`index.html`을 브라우저에서 직접 열거나 Live Server로 실행합니다.

## AI 활용 기록

1. 화면 구조 생성 요청
2. Todo 추가 기능 구현 요청
3. 완료/삭제 기능 구현 요청
4. localStorage 저장 기능 구현 요청

## 배운 점

- DOM 선택
- 이벤트 연결
- 배열과 객체로 Todo 관리
- `renderTodos()`로 화면 갱신
- `JSON.stringify()` / `JSON.parse()`로 localStorage 저장
=======
# Todo App

HTML, CSS, Vanilla JavaScript, Tailwind CDN으로 만든 간단한 Todo App입니다.

## 사용 기술

- HTML
- CSS
- Vanilla JavaScript
- Tailwind CDN

## 주요 기능

- Todo 추가
- Enter 키로 추가
- 빈 값 방지
- Todo 목록 표시
- 완료/미완료 변경
- Todo 삭제
- Total / Completed / Remaining 개수 표시
- localStorage 저장으로 새로고침 후 유지

## 실행 방법

`index.html`을 브라우저에서 직접 열거나 Live Server로 실행합니다.

## AI 활용 기록

1. 화면 구조 생성 요청
2. Todo 추가 기능 구현 요청
3. 완료/삭제 기능 구현 요청
4. localStorage 저장 기능 구현 요청

## 배운 점

- DOM 선택
- 이벤트 연결
- 배열과 객체로 Todo 관리
- `renderTodos()`로 화면 갱신
- `JSON.stringify()` / `JSON.parse()`로 localStorage 저장
>>>>>>> origin/main
